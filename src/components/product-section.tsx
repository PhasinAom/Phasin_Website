import { FeatureCarousel } from '@/components/ui/feature-carousel';

export function ProductSection() {
  return (
    <section aria-label="Things That I Build">
      <FeatureCarousel />
    </section>
  );
}
